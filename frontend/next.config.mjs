/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  output: "export",
  basePath: "/car_taxi",
  images: {
    unoptimized: true,
  }
};

export default nextConfig;
